前端发起 POST /api/v1/chat/question
        │
        ▼
question_answer()  [chat.py#L247]
        │
        ▼
question_answer_inner()  [chat.py#L253]
  ├─ 解析快速命令 (/regenerate /analysis /predict)
  │
  ▼
stream_sql()  [chat.py#L346]
  ├─ LLMService.create()  ← ✅ 初始化：加载模型配置、工作空间权限
  ├─ llm_service.init_record()  ← 保存用户问题到 chat_record 表
  └─ llm_service.run_task_async()  ← 提交到线程池异步执行
        │
        ▼
run_task()  [llm.py#L1228-L1543]  ★ 最核心的方法
  │
  ├─ ① FILTER_TERMS          [本地操作，不调AI]  ← 术语匹配
  ├─ ② FILTER_SQL_EXAMPLE    [本地操作，不调AI]  ← SQL示例召回 (Embedding)
  ├─ ③ FILTER_CUSTOM_PROMPT  [本地操作，不调AI]  ← 自定义提示词
  ├─ ④ init_messages()       ← 组装 Prompt：系统提示+历史对话+表结构
  │
  ├─ ⑤ CHOOSE_DATASOURCE?    ← ✅ AI 调用 #1（可选，仅当未选数据源时）
  │     └─ llm.stream() → 让 AI 从多个数据源中选一个
  │
  ├─ ⑥ GENERATE_SQL          ← ✅ AI 调用 #2（必选）
  │     └─ llm.stream(sql_message) → 让 AI 生成 SQL（含表名+chart-type+标题）
  │
  ├─ ⑦ GENERATE_SQL_WITH_PERMISSIONS?  ← ✅ AI 调用 #3（可选，普通用户行级权限）
  │     └─ llm.stream() → 注入行级权限过滤条件
  │     或者
  │   GENERATE_DYNAMIC_SQL?  ← ✅ AI 调用 #3（可选，动态数据源Assistant）
  │
  ├─ ⑧ EXECUTE_SQL           [本地操作]  ← 用 exec_sql() 执行真实数据库查询
  │
  ├─ ⑨ GENERATE_CHART        ← ✅ AI 调用 #4（必选）
  │     └─ llm.stream(chart_message) → 生成图表配置 JSON (x轴/y轴/图表类型)
  │
  └─ ⑩ GENERATE_PICTURE      [本地操作]  ← 调用图表服务生成图片 URL (可选)