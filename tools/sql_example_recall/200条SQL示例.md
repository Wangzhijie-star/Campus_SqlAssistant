# 200 条校园 SQL 示例（待业务复核）

依据四份 XLS 的第二行字段设计。`{{history}}`、`{{current}}`、`{{detail}}`、`{{ranking}}` 必须替换为导入后的真实表名。数值字段须正确导入为数值类型。尚未执行数据库校验或真实召回。

## E001 查看全部曾不及格记录，按学号排序

```sql
SELECT * FROM {{history}} ORDER BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E002 曾不及格记录共有多少行

```sql
SELECT COUNT(*) AS "记录数" FROM {{history}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。记录数不等于课程数或人数。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E003 曾不及格记录涉及多少名不同学生

```sql
SELECT COUNT(DISTINCT "学号") AS "人数" FROM {{history}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E004 列出曾不及格记录中出现的学生学号

```sql
SELECT DISTINCT "学号" FROM {{history}} WHERE "学号" IS NOT NULL ORDER BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E005 统计曾不及格记录中每名学生的记录行数

```sql
SELECT "学号", COUNT(*) AS "记录数" FROM {{history}} GROUP BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E006 找出曾不及格记录中有多行记录的学生学号

```sql
SELECT "学号", COUNT(*) AS "记录数" FROM {{history}} GROUP BY "学号" HAVING COUNT(*) > 1;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。此查询不把多行直接判定为重复数据。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E007 按学年学期统计曾不及格记录的记录数

```sql
SELECT "学年学期", COUNT(*) AS "记录数" FROM {{history}} GROUP BY "学年学期";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E008 按学年学期统计曾不及格记录涉及的去重学生人数

```sql
SELECT "学年学期", COUNT(DISTINCT "学号") AS "人数" FROM {{history}} GROUP BY "学年学期";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E009 按班级统计曾不及格记录的记录数

```sql
SELECT "班级", COUNT(*) AS "记录数" FROM {{history}} GROUP BY "班级";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E010 按班级统计曾不及格记录涉及的去重学生人数

```sql
SELECT "班级", COUNT(DISTINCT "学号") AS "人数" FROM {{history}} GROUP BY "班级";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E011 按专业方向统计曾不及格记录的记录数

```sql
SELECT "专业方向", COUNT(*) AS "记录数" FROM {{history}} GROUP BY "专业方向";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E012 按专业方向统计曾不及格记录涉及的去重学生人数

```sql
SELECT "专业方向", COUNT(DISTINCT "学号") AS "人数" FROM {{history}} GROUP BY "专业方向";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E013 按专业统计曾不及格记录的记录数

```sql
SELECT "专业", COUNT(*) AS "记录数" FROM {{history}} GROUP BY "专业";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E014 按专业统计曾不及格记录涉及的去重学生人数

```sql
SELECT "专业", COUNT(DISTINCT "学号") AS "人数" FROM {{history}} GROUP BY "专业";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E015 按学院统计曾不及格记录的记录数

```sql
SELECT "学院", COUNT(*) AS "记录数" FROM {{history}} GROUP BY "学院";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E016 按学院统计曾不及格记录涉及的去重学生人数

```sql
SELECT "学院", COUNT(DISTINCT "学号") AS "人数" FROM {{history}} GROUP BY "学院";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E017 按课程号统计曾不及格记录的记录数

```sql
SELECT "课程号", COUNT(*) AS "记录数" FROM {{history}} GROUP BY "课程号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E018 按课程号统计曾不及格记录涉及的去重学生人数

```sql
SELECT "课程号", COUNT(DISTINCT "学号") AS "人数" FROM {{history}} GROUP BY "课程号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E019 按课程名统计曾不及格记录的记录数

```sql
SELECT "课程名", COUNT(*) AS "记录数" FROM {{history}} GROUP BY "课程名";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E020 按课程名统计曾不及格记录涉及的去重学生人数

```sql
SELECT "课程名", COUNT(DISTINCT "学号") AS "人数" FROM {{history}} GROUP BY "课程名";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E021 计算曾不及格记录的学分平均值

```sql
SELECT AVG(CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{history}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E022 计算曾不及格记录的学分最大值

```sql
SELECT MAX(CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{history}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E023 计算曾不及格记录的学分最小值

```sql
SELECT MIN(CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{history}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E024 列出曾不及格记录中学分高于本表平均值的记录

```sql
SELECT * FROM {{history}} WHERE CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC) > (SELECT AVG(CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC)) FROM {{history}});
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E025 列出曾不及格记录中分数为空的行

```sql
SELECT * FROM {{history}} WHERE "课程成绩" IS NULL;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E026 列出曾不及格记录中分数为零的行

```sql
SELECT * FROM {{history}} WHERE (CASE WHEN TRIM(CAST("课程成绩" AS TEXT)) ~ '^[0-9]+([.][0-9]+)?$' THEN CAST(TRIM(CAST("课程成绩" AS TEXT)) AS NUMERIC) END) = 0;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E027 列出曾不及格记录中数值分数低于60的行

```sql
SELECT * FROM {{history}} WHERE (CASE WHEN TRIM(CAST("课程成绩" AS TEXT)) ~ '^[0-9]+([.][0-9]+)?$' THEN CAST(TRIM(CAST("课程成绩" AS TEXT)) AS NUMERIC) END) < 60;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E028 列出曾不及格记录中数值分数达到60的行

```sql
SELECT * FROM {{history}} WHERE (CASE WHEN TRIM(CAST("课程成绩" AS TEXT)) ~ '^[0-9]+([.][0-9]+)?$' THEN CAST(TRIM(CAST("课程成绩" AS TEXT)) AS NUMERIC) END) >= 60;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E029 列出曾不及格记录中数值分数达到90的行

```sql
SELECT * FROM {{history}} WHERE (CASE WHEN TRIM(CAST("课程成绩" AS TEXT)) ~ '^[0-9]+([.][0-9]+)?$' THEN CAST(TRIM(CAST("课程成绩" AS TEXT)) AS NUMERIC) END) >= 90;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E030 曾不及格记录包含多少门不同课程

```sql
SELECT COUNT(DISTINCT "课程号") AS "课程数" FROM {{history}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E031 按学号统计曾不及格记录中的不同课程数量

```sql
SELECT "学号", COUNT(DISTINCT "课程号") AS "课程数" FROM {{history}} GROUP BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E032 找出曾不及格记录中同一学生同一课程出现多次的组合

```sql
SELECT "学号", "课程号", COUNT(*) AS "记录数" FROM {{history}} GROUP BY "学号", "课程号" HAVING COUNT(*) > 1;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。多次出现不自动等于错误重复或考试次数。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E033 查看全部尚不及格记录，按学号排序

```sql
SELECT * FROM {{current}} ORDER BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E034 尚不及格记录共有多少行

```sql
SELECT COUNT(*) AS "记录数" FROM {{current}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。记录数不等于课程数或人数。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E035 尚不及格记录涉及多少名不同学生

```sql
SELECT COUNT(DISTINCT "学号") AS "人数" FROM {{current}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E036 列出尚不及格记录中出现的学生学号

```sql
SELECT DISTINCT "学号" FROM {{current}} WHERE "学号" IS NOT NULL ORDER BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E037 统计尚不及格记录中每名学生的记录行数

```sql
SELECT "学号", COUNT(*) AS "记录数" FROM {{current}} GROUP BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E038 找出尚不及格记录中有多行记录的学生学号

```sql
SELECT "学号", COUNT(*) AS "记录数" FROM {{current}} GROUP BY "学号" HAVING COUNT(*) > 1;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。此查询不把多行直接判定为重复数据。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E039 按院系统计尚不及格记录的记录数

```sql
SELECT "院系", COUNT(*) AS "记录数" FROM {{current}} GROUP BY "院系";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E040 按院系统计尚不及格记录涉及的去重学生人数

```sql
SELECT "院系", COUNT(DISTINCT "学号") AS "人数" FROM {{current}} GROUP BY "院系";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E041 按专业名统计尚不及格记录的记录数

```sql
SELECT "专业名", COUNT(*) AS "记录数" FROM {{current}} GROUP BY "专业名";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E042 按专业名统计尚不及格记录涉及的去重学生人数

```sql
SELECT "专业名", COUNT(DISTINCT "学号") AS "人数" FROM {{current}} GROUP BY "专业名";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E043 按专业方向统计尚不及格记录的记录数

```sql
SELECT "专业方向", COUNT(*) AS "记录数" FROM {{current}} GROUP BY "专业方向";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E044 按专业方向统计尚不及格记录涉及的去重学生人数

```sql
SELECT "专业方向", COUNT(DISTINCT "学号") AS "人数" FROM {{current}} GROUP BY "专业方向";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E045 按班级号统计尚不及格记录的记录数

```sql
SELECT "班级号", COUNT(*) AS "记录数" FROM {{current}} GROUP BY "班级号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E046 按班级号统计尚不及格记录涉及的去重学生人数

```sql
SELECT "班级号", COUNT(DISTINCT "学号") AS "人数" FROM {{current}} GROUP BY "班级号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E047 按班名统计尚不及格记录的记录数

```sql
SELECT "班名", COUNT(*) AS "记录数" FROM {{current}} GROUP BY "班名";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E048 按班名统计尚不及格记录涉及的去重学生人数

```sql
SELECT "班名", COUNT(DISTINCT "学号") AS "人数" FROM {{current}} GROUP BY "班名";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E049 按入学年级统计尚不及格记录的记录数

```sql
SELECT "入学年级", COUNT(*) AS "记录数" FROM {{current}} GROUP BY "入学年级";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E050 按入学年级统计尚不及格记录涉及的去重学生人数

```sql
SELECT "入学年级", COUNT(DISTINCT "学号") AS "人数" FROM {{current}} GROUP BY "入学年级";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E051 按课程名统计尚不及格记录的记录数

```sql
SELECT "课程名", COUNT(*) AS "记录数" FROM {{current}} GROUP BY "课程名";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E052 按课程名统计尚不及格记录涉及的去重学生人数

```sql
SELECT "课程名", COUNT(DISTINCT "学号") AS "人数" FROM {{current}} GROUP BY "课程名";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E053 计算尚不及格记录的学分平均值

```sql
SELECT AVG(CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{current}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E054 计算尚不及格记录的学分最大值

```sql
SELECT MAX(CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{current}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E055 计算尚不及格记录的学分最小值

```sql
SELECT MIN(CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{current}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E056 列出尚不及格记录中学分高于本表平均值的记录

```sql
SELECT * FROM {{current}} WHERE CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC) > (SELECT AVG(CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC)) FROM {{current}});
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E057 计算尚不及格记录的不及格次数平均值

```sql
SELECT AVG(CAST(NULLIF(TRIM(CAST("不及格次数" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{current}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E058 计算尚不及格记录的不及格次数最大值

```sql
SELECT MAX(CAST(NULLIF(TRIM(CAST("不及格次数" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{current}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E059 计算尚不及格记录的不及格次数最小值

```sql
SELECT MIN(CAST(NULLIF(TRIM(CAST("不及格次数" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{current}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E060 列出尚不及格记录中不及格次数高于本表平均值的记录

```sql
SELECT * FROM {{current}} WHERE CAST(NULLIF(TRIM(CAST("不及格次数" AS TEXT)), '') AS NUMERIC) > (SELECT AVG(CAST(NULLIF(TRIM(CAST("不及格次数" AS TEXT)), '') AS NUMERIC)) FROM {{current}});
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E061 列出尚不及格记录中分数为空的行

```sql
SELECT * FROM {{current}} WHERE "成绩" IS NULL;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E062 列出尚不及格记录中分数为零的行

```sql
SELECT * FROM {{current}} WHERE (CASE WHEN TRIM(CAST("成绩" AS TEXT)) ~ '^[0-9]+([.][0-9]+)?$' THEN CAST(TRIM(CAST("成绩" AS TEXT)) AS NUMERIC) END) = 0;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E063 列出尚不及格记录中数值分数低于60的行

```sql
SELECT * FROM {{current}} WHERE (CASE WHEN TRIM(CAST("成绩" AS TEXT)) ~ '^[0-9]+([.][0-9]+)?$' THEN CAST(TRIM(CAST("成绩" AS TEXT)) AS NUMERIC) END) < 60;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E064 列出尚不及格记录中数值分数达到60的行

```sql
SELECT * FROM {{current}} WHERE (CASE WHEN TRIM(CAST("成绩" AS TEXT)) ~ '^[0-9]+([.][0-9]+)?$' THEN CAST(TRIM(CAST("成绩" AS TEXT)) AS NUMERIC) END) >= 60;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E065 列出尚不及格记录中数值分数达到90的行

```sql
SELECT * FROM {{current}} WHERE (CASE WHEN TRIM(CAST("成绩" AS TEXT)) ~ '^[0-9]+([.][0-9]+)?$' THEN CAST(TRIM(CAST("成绩" AS TEXT)) AS NUMERIC) END) >= 90;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E066 尚不及格记录包含多少门不同课程

```sql
SELECT COUNT(DISTINCT "课程号") AS "课程数" FROM {{current}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E067 按学号统计尚不及格记录中的不同课程数量

```sql
SELECT "学号", COUNT(DISTINCT "课程号") AS "课程数" FROM {{current}} GROUP BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E068 找出尚不及格记录中同一学生同一课程出现多次的组合

```sql
SELECT "学号", "课程号", COUNT(*) AS "记录数" FROM {{current}} GROUP BY "学号", "课程号" HAVING COUNT(*) > 1;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。多次出现不自动等于错误重复或考试次数。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E069 查看全部成绩明细记录，按学号排序

```sql
SELECT * FROM {{detail}} ORDER BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E070 成绩明细记录共有多少行

```sql
SELECT COUNT(*) AS "记录数" FROM {{detail}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。记录数不等于课程数或人数。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E071 成绩明细记录涉及多少名不同学生

```sql
SELECT COUNT(DISTINCT "学号") AS "人数" FROM {{detail}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E072 列出成绩明细记录中出现的学生学号

```sql
SELECT DISTINCT "学号" FROM {{detail}} WHERE "学号" IS NOT NULL ORDER BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E073 统计成绩明细记录中每名学生的记录行数

```sql
SELECT "学号", COUNT(*) AS "记录数" FROM {{detail}} GROUP BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E074 找出成绩明细记录中有多行记录的学生学号

```sql
SELECT "学号", COUNT(*) AS "记录数" FROM {{detail}} GROUP BY "学号" HAVING COUNT(*) > 1;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。此查询不把多行直接判定为重复数据。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E075 按课程号统计成绩明细记录的记录数

```sql
SELECT "课程号", COUNT(*) AS "记录数" FROM {{detail}} GROUP BY "课程号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E076 按课程号统计成绩明细记录涉及的去重学生人数

```sql
SELECT "课程号", COUNT(DISTINCT "学号") AS "人数" FROM {{detail}} GROUP BY "课程号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E077 按课序号统计成绩明细记录的记录数

```sql
SELECT "课序号", COUNT(*) AS "记录数" FROM {{detail}} GROUP BY "课序号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E078 按课序号统计成绩明细记录涉及的去重学生人数

```sql
SELECT "课序号", COUNT(DISTINCT "学号") AS "人数" FROM {{detail}} GROUP BY "课序号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E079 按课程名统计成绩明细记录的记录数

```sql
SELECT "课程名", COUNT(*) AS "记录数" FROM {{detail}} GROUP BY "课程名";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E080 按课程名统计成绩明细记录涉及的去重学生人数

```sql
SELECT "课程名", COUNT(DISTINCT "学号") AS "人数" FROM {{detail}} GROUP BY "课程名";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E081 按考试时间统计成绩明细记录的记录数

```sql
SELECT "考试时间", COUNT(*) AS "记录数" FROM {{detail}} GROUP BY "考试时间";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E082 按考试时间统计成绩明细记录涉及的去重学生人数

```sql
SELECT "考试时间", COUNT(DISTINCT "学号") AS "人数" FROM {{detail}} GROUP BY "考试时间";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E083 按修读方式统计成绩明细记录的记录数

```sql
SELECT "修读方式", COUNT(*) AS "记录数" FROM {{detail}} GROUP BY "修读方式";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E084 按修读方式统计成绩明细记录涉及的去重学生人数

```sql
SELECT "修读方式", COUNT(DISTINCT "学号") AS "人数" FROM {{detail}} GROUP BY "修读方式";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E085 按课程属性统计成绩明细记录的记录数

```sql
SELECT "课程属性", COUNT(*) AS "记录数" FROM {{detail}} GROUP BY "课程属性";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E086 按课程属性统计成绩明细记录涉及的去重学生人数

```sql
SELECT "课程属性", COUNT(DISTINCT "学号") AS "人数" FROM {{detail}} GROUP BY "课程属性";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E087 按班级统计成绩明细记录的记录数

```sql
SELECT "班级", COUNT(*) AS "记录数" FROM {{detail}} GROUP BY "班级";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E088 按班级统计成绩明细记录涉及的去重学生人数

```sql
SELECT "班级", COUNT(DISTINCT "学号") AS "人数" FROM {{detail}} GROUP BY "班级";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E089 计算成绩明细记录的学分平均值

```sql
SELECT AVG(CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{detail}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E090 计算成绩明细记录的学分最大值

```sql
SELECT MAX(CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{detail}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E091 计算成绩明细记录的学分最小值

```sql
SELECT MIN(CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{detail}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E092 列出成绩明细记录中学分高于本表平均值的记录

```sql
SELECT * FROM {{detail}} WHERE CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC) > (SELECT AVG(CAST(NULLIF(TRIM(CAST("学分" AS TEXT)), '') AS NUMERIC)) FROM {{detail}});
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E093 列出成绩明细记录中分数为空的行

```sql
SELECT * FROM {{detail}} WHERE "总成绩" IS NULL;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E094 列出成绩明细记录中分数为零的行

```sql
SELECT * FROM {{detail}} WHERE (CASE WHEN TRIM(CAST("总成绩" AS TEXT)) ~ '^[0-9]+([.][0-9]+)?$' THEN CAST(TRIM(CAST("总成绩" AS TEXT)) AS NUMERIC) END) = 0;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E095 列出成绩明细记录中数值分数低于60的行

```sql
SELECT * FROM {{detail}} WHERE (CASE WHEN TRIM(CAST("总成绩" AS TEXT)) ~ '^[0-9]+([.][0-9]+)?$' THEN CAST(TRIM(CAST("总成绩" AS TEXT)) AS NUMERIC) END) < 60;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E096 列出成绩明细记录中数值分数达到60的行

```sql
SELECT * FROM {{detail}} WHERE (CASE WHEN TRIM(CAST("总成绩" AS TEXT)) ~ '^[0-9]+([.][0-9]+)?$' THEN CAST(TRIM(CAST("总成绩" AS TEXT)) AS NUMERIC) END) >= 60;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E097 列出成绩明细记录中数值分数达到90的行

```sql
SELECT * FROM {{detail}} WHERE (CASE WHEN TRIM(CAST("总成绩" AS TEXT)) ~ '^[0-9]+([.][0-9]+)?$' THEN CAST(TRIM(CAST("总成绩" AS TEXT)) AS NUMERIC) END) >= 90;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。只对数值分数做区间判断，等级制不擅自换算；零分纳入低于60的条件。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E098 成绩明细记录包含多少门不同课程

```sql
SELECT COUNT(DISTINCT "课程号") AS "课程数" FROM {{detail}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E099 按学号统计成绩明细记录中的不同课程数量

```sql
SELECT "学号", COUNT(DISTINCT "课程号") AS "课程数" FROM {{detail}} GROUP BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E100 找出成绩明细记录中同一学生同一课程出现多次的组合

```sql
SELECT "学号", "课程号", COUNT(*) AS "记录数" FROM {{detail}} GROUP BY "学号", "课程号" HAVING COUNT(*) > 1;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。多次出现不自动等于错误重复或考试次数。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E101 查看全部加权排名表学生，按学号排序

```sql
SELECT * FROM {{ranking}} ORDER BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E102 加权排名表学生共有多少行

```sql
SELECT COUNT(*) AS "记录数" FROM {{ranking}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。记录数不等于课程数或人数。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E103 加权排名表学生涉及多少名不同学生

```sql
SELECT COUNT(DISTINCT "学号") AS "人数" FROM {{ranking}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E104 列出加权排名表学生中出现的学生学号

```sql
SELECT DISTINCT "学号" FROM {{ranking}} WHERE "学号" IS NOT NULL ORDER BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E105 统计加权排名表学生中每名学生的记录行数

```sql
SELECT "学号", COUNT(*) AS "记录数" FROM {{ranking}} GROUP BY "学号";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E106 找出加权排名表学生中有多行记录的学生学号

```sql
SELECT "学号", COUNT(*) AS "记录数" FROM {{ranking}} GROUP BY "学号" HAVING COUNT(*) > 1;
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。此查询不把多行直接判定为重复数据。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E107 按班级统计加权排名表学生的记录数

```sql
SELECT "班级", COUNT(*) AS "记录数" FROM {{ranking}} GROUP BY "班级";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E108 按班级统计加权排名表学生涉及的去重学生人数

```sql
SELECT "班级", COUNT(DISTINCT "学号") AS "人数" FROM {{ranking}} GROUP BY "班级";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E109 按专业方向统计加权排名表学生的记录数

```sql
SELECT "专业方向", COUNT(*) AS "记录数" FROM {{ranking}} GROUP BY "专业方向";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E110 按专业方向统计加权排名表学生涉及的去重学生人数

```sql
SELECT "专业方向", COUNT(DISTINCT "学号") AS "人数" FROM {{ranking}} GROUP BY "专业方向";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E111 按专业统计加权排名表学生的记录数

```sql
SELECT "专业", COUNT(*) AS "记录数" FROM {{ranking}} GROUP BY "专业";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E112 按专业统计加权排名表学生涉及的去重学生人数

```sql
SELECT "专业", COUNT(DISTINCT "学号") AS "人数" FROM {{ranking}} GROUP BY "专业";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E113 按院系统计加权排名表学生的记录数

```sql
SELECT "院系", COUNT(*) AS "记录数" FROM {{ranking}} GROUP BY "院系";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E114 按院系统计加权排名表学生涉及的去重学生人数

```sql
SELECT "院系", COUNT(DISTINCT "学号") AS "人数" FROM {{ranking}} GROUP BY "院系";
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E115 计算加权排名表学生的GPA平均值

```sql
SELECT AVG(CAST(NULLIF(TRIM(CAST("GPA" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{ranking}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E116 计算加权排名表学生的GPA最大值

```sql
SELECT MAX(CAST(NULLIF(TRIM(CAST("GPA" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{ranking}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E117 计算加权排名表学生的GPA最小值

```sql
SELECT MIN(CAST(NULLIF(TRIM(CAST("GPA" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{ranking}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E118 列出加权排名表学生中GPA高于本表平均值的记录

```sql
SELECT * FROM {{ranking}} WHERE CAST(NULLIF(TRIM(CAST("GPA" AS TEXT)), '') AS NUMERIC) > (SELECT AVG(CAST(NULLIF(TRIM(CAST("GPA" AS TEXT)), '') AS NUMERIC)) FROM {{ranking}});
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E119 计算加权排名表学生的加权学分成绩平均值

```sql
SELECT AVG(CAST(NULLIF(TRIM(CAST("加权学分成绩" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{ranking}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E120 计算加权排名表学生的加权学分成绩最大值

```sql
SELECT MAX(CAST(NULLIF(TRIM(CAST("加权学分成绩" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{ranking}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E121 计算加权排名表学生的加权学分成绩最小值

```sql
SELECT MIN(CAST(NULLIF(TRIM(CAST("加权学分成绩" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{ranking}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E122 列出加权排名表学生中加权学分成绩高于本表平均值的记录

```sql
SELECT * FROM {{ranking}} WHERE CAST(NULLIF(TRIM(CAST("加权学分成绩" AS TEXT)), '') AS NUMERIC) > (SELECT AVG(CAST(NULLIF(TRIM(CAST("加权学分成绩" AS TEXT)), '') AS NUMERIC)) FROM {{ranking}});
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E123 计算加权排名表学生的平均成绩平均值

```sql
SELECT AVG(CAST(NULLIF(TRIM(CAST("平均成绩" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{ranking}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E124 计算加权排名表学生的平均成绩最大值

```sql
SELECT MAX(CAST(NULLIF(TRIM(CAST("平均成绩" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{ranking}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E125 计算加权排名表学生的平均成绩最小值

```sql
SELECT MIN(CAST(NULLIF(TRIM(CAST("平均成绩" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{ranking}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E126 列出加权排名表学生中平均成绩高于本表平均值的记录

```sql
SELECT * FROM {{ranking}} WHERE CAST(NULLIF(TRIM(CAST("平均成绩" AS TEXT)), '') AS NUMERIC) > (SELECT AVG(CAST(NULLIF(TRIM(CAST("平均成绩" AS TEXT)), '') AS NUMERIC)) FROM {{ranking}});
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E127 计算加权排名表学生的尚不及格学分平均值

```sql
SELECT AVG(CAST(NULLIF(TRIM(CAST("尚不及格学分" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{ranking}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E128 计算加权排名表学生的尚不及格学分最大值

```sql
SELECT MAX(CAST(NULLIF(TRIM(CAST("尚不及格学分" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{ranking}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E129 计算加权排名表学生的尚不及格学分最小值

```sql
SELECT MIN(CAST(NULLIF(TRIM(CAST("尚不及格学分" AS TEXT)), '') AS NUMERIC)) AS "结果" FROM {{ranking}};
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。忽略空值，不把空值补为0。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E130 列出加权排名表学生中尚不及格学分高于本表平均值的记录

```sql
SELECT * FROM {{ranking}} WHERE CAST(NULLIF(TRIM(CAST("尚不及格学分" AS TEXT)), '') AS NUMERIC) > (SELECT AVG(CAST(NULLIF(TRIM(CAST("尚不及格学分" AS TEXT)), '') AS NUMERIC)) FROM {{ranking}});
```

仅统计该导入快照；学号按文本保存。曾不及格不等于当前挂科。排名表指标使用原表值。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E131 列出原表GPA专业排名前十名学生及名次

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA专业排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} WHERE CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA专业排名" AS TEXT)), '/', 1), '') AS NUMERIC) BETWEEN 1 AND 10 ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA专业排名" AS TEXT)), '/', 1), '') AS NUMERIC), "学号";
```

按原表名次<=10筛选，保留并列；不在单班样本内重算专业名次。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E132 查看原表每名学生的GPA专业排名

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA专业排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA专业排名" AS TEXT)), '/', 1), '') AS NUMERIC) NULLS LAST, "学号";
```

读取已有排名，不从当前样本重新计算。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E133 列出原表GPA专业方向排名前十名学生及名次

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} WHERE CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC) BETWEEN 1 AND 10 ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC), "学号";
```

按原表名次<=10筛选，保留并列；不在单班样本内重算专业名次。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E134 查看原表每名学生的GPA专业方向排名

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC) NULLS LAST, "学号";
```

读取已有排名，不从当前样本重新计算。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E135 列出原表GPA班级排名前十名学生及名次

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA班级排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} WHERE CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA班级排名" AS TEXT)), '/', 1), '') AS NUMERIC) BETWEEN 1 AND 10 ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA班级排名" AS TEXT)), '/', 1), '') AS NUMERIC), "学号";
```

按原表名次<=10筛选，保留并列；不在单班样本内重算专业名次。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E136 查看原表每名学生的GPA班级排名

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA班级排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA班级排名" AS TEXT)), '/', 1), '') AS NUMERIC) NULLS LAST, "学号";
```

读取已有排名，不从当前样本重新计算。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E137 列出原表加权专业排名前十名学生及名次

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权专业排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} WHERE CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权专业排名" AS TEXT)), '/', 1), '') AS NUMERIC) BETWEEN 1 AND 10 ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权专业排名" AS TEXT)), '/', 1), '') AS NUMERIC), "学号";
```

按原表名次<=10筛选，保留并列；不在单班样本内重算专业名次。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E138 查看原表每名学生的加权专业排名

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权专业排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权专业排名" AS TEXT)), '/', 1), '') AS NUMERIC) NULLS LAST, "学号";
```

读取已有排名，不从当前样本重新计算。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E139 列出原表加权专业方向排名前十名学生及名次

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} WHERE CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC) BETWEEN 1 AND 10 ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC), "学号";
```

按原表名次<=10筛选，保留并列；不在单班样本内重算专业名次。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E140 查看原表每名学生的加权专业方向排名

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC) NULLS LAST, "学号";
```

读取已有排名，不从当前样本重新计算。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E141 列出原表加权班级排名前十名学生及名次

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权班级排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} WHERE CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权班级排名" AS TEXT)), '/', 1), '') AS NUMERIC) BETWEEN 1 AND 10 ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权班级排名" AS TEXT)), '/', 1), '') AS NUMERIC), "学号";
```

按原表名次<=10筛选，保留并列；不在单班样本内重算专业名次。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E142 查看原表每名学生的加权班级排名

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权班级排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权班级排名" AS TEXT)), '/', 1), '') AS NUMERIC) NULLS LAST, "学号";
```

读取已有排名，不从当前样本重新计算。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E143 列出原表平均成绩专业排名前十名学生及名次

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩专业排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} WHERE CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩专业排名" AS TEXT)), '/', 1), '') AS NUMERIC) BETWEEN 1 AND 10 ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩专业排名" AS TEXT)), '/', 1), '') AS NUMERIC), "学号";
```

按原表名次<=10筛选，保留并列；不在单班样本内重算专业名次。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E144 查看原表每名学生的平均成绩专业排名

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩专业排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩专业排名" AS TEXT)), '/', 1), '') AS NUMERIC) NULLS LAST, "学号";
```

读取已有排名，不从当前样本重新计算。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E145 列出原表平均成绩专业方向排名前十名学生及名次

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} WHERE CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC) BETWEEN 1 AND 10 ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC), "学号";
```

按原表名次<=10筛选，保留并列；不在单班样本内重算专业名次。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E146 查看原表每名学生的平均成绩专业方向排名

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩专业方向排名" AS TEXT)), '/', 1), '') AS NUMERIC) NULLS LAST, "学号";
```

读取已有排名，不从当前样本重新计算。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E147 列出原表平均成绩班级排名前十名学生及名次

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩班级排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} WHERE CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩班级排名" AS TEXT)), '/', 1), '') AS NUMERIC) BETWEEN 1 AND 10 ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩班级排名" AS TEXT)), '/', 1), '') AS NUMERIC), "学号";
```

按原表名次<=10筛选，保留并列；不在单班样本内重算专业名次。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E148 查看原表每名学生的平均成绩班级排名

```sql
SELECT "学号", "姓名", CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩班级排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} ORDER BY CAST(NULLIF(SPLIT_PART(TRIM(CAST("平均成绩班级排名" AS TEXT)), '/', 1), '') AS NUMERIC) NULLS LAST, "学号";
```

读取已有排名，不从当前样本重新计算。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E149 查看各学生原表中的要求总学分

```sql
SELECT "学号", "姓名", CAST(NULLIF(TRIM(CAST("要求总学分" AS TEXT)), '') AS NUMERIC) FROM {{ranking}} ORDER BY "学号";
```

直接读取报表指标，不能用明细记录学分求和替代。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E150 按班级计算原表要求总学分的平均值

```sql
SELECT "班级", AVG(CAST(NULLIF(TRIM(CAST("要求总学分" AS TEXT)), '') AS NUMERIC)) AS "平均值" FROM {{ranking}} GROUP BY "班级";
```

只对非空数值求平均。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E151 查看各学生原表中的已修课程学分

```sql
SELECT "学号", "姓名", CAST(NULLIF(TRIM(CAST("已修课程学分" AS TEXT)), '') AS NUMERIC) FROM {{ranking}} ORDER BY "学号";
```

直接读取报表指标，不能用明细记录学分求和替代。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E152 按班级计算原表已修课程学分的平均值

```sql
SELECT "班级", AVG(CAST(NULLIF(TRIM(CAST("已修课程学分" AS TEXT)), '') AS NUMERIC)) AS "平均值" FROM {{ranking}} GROUP BY "班级";
```

只对非空数值求平均。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E153 查看各学生原表中的已修自主实践学分

```sql
SELECT "学号", "姓名", CAST(NULLIF(TRIM(CAST("已修自主实践学分" AS TEXT)), '') AS NUMERIC) FROM {{ranking}} ORDER BY "学号";
```

直接读取报表指标，不能用明细记录学分求和替代。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E154 按班级计算原表已修自主实践学分的平均值

```sql
SELECT "班级", AVG(CAST(NULLIF(TRIM(CAST("已修自主实践学分" AS TEXT)), '') AS NUMERIC)) AS "平均值" FROM {{ranking}} GROUP BY "班级";
```

只对非空数值求平均。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E155 查看各学生原表中的曾不及格学分

```sql
SELECT "学号", "姓名", CAST(NULLIF(TRIM(CAST("曾不及格学分" AS TEXT)), '') AS NUMERIC) FROM {{ranking}} ORDER BY "学号";
```

直接读取报表指标，不能用明细记录学分求和替代。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E156 按班级计算原表曾不及格学分的平均值

```sql
SELECT "班级", AVG(CAST(NULLIF(TRIM(CAST("曾不及格学分" AS TEXT)), '') AS NUMERIC)) AS "平均值" FROM {{ranking}} GROUP BY "班级";
```

只对非空数值求平均。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E157 查看各学生原表中的尚不及格学分

```sql
SELECT "学号", "姓名", CAST(NULLIF(TRIM(CAST("尚不及格学分" AS TEXT)), '') AS NUMERIC) FROM {{ranking}} ORDER BY "学号";
```

直接读取报表指标，不能用明细记录学分求和替代。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E158 按班级计算原表尚不及格学分的平均值

```sql
SELECT "班级", AVG(CAST(NULLIF(TRIM(CAST("尚不及格学分" AS TEXT)), '') AS NUMERIC)) AS "平均值" FROM {{ranking}} GROUP BY "班级";
```

只对非空数值求平均。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E159 列出学号：曾不及格表中，在尚不及格表存在记录的学生

```sql
SELECT DISTINCT a."学号" FROM {{history}} a WHERE a."学号" IS NOT NULL AND EXISTS (SELECT 1 FROM {{current}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E160 统计人数：曾不及格表中，在尚不及格表存在记录的学生

```sql
SELECT COUNT(DISTINCT a."学号") AS "人数" FROM {{history}} a WHERE a."学号" IS NOT NULL AND EXISTS (SELECT 1 FROM {{current}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E161 列出学号：曾不及格表中，在尚不及格表不存在记录的学生

```sql
SELECT DISTINCT a."学号" FROM {{history}} a WHERE a."学号" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM {{current}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E162 统计人数：曾不及格表中，在尚不及格表不存在记录的学生

```sql
SELECT COUNT(DISTINCT a."学号") AS "人数" FROM {{history}} a WHERE a."学号" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM {{current}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E163 列出学号：尚不及格表中，在曾不及格表存在记录的学生

```sql
SELECT DISTINCT a."学号" FROM {{current}} a WHERE a."学号" IS NOT NULL AND EXISTS (SELECT 1 FROM {{history}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E164 统计人数：尚不及格表中，在曾不及格表存在记录的学生

```sql
SELECT COUNT(DISTINCT a."学号") AS "人数" FROM {{current}} a WHERE a."学号" IS NOT NULL AND EXISTS (SELECT 1 FROM {{history}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E165 列出学号：尚不及格表中，在曾不及格表不存在记录的学生

```sql
SELECT DISTINCT a."学号" FROM {{current}} a WHERE a."学号" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM {{history}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E166 统计人数：尚不及格表中，在曾不及格表不存在记录的学生

```sql
SELECT COUNT(DISTINCT a."学号") AS "人数" FROM {{current}} a WHERE a."学号" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM {{history}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E167 列出学号：排名表表中，在尚不及格表存在记录的学生

```sql
SELECT DISTINCT a."学号" FROM {{ranking}} a WHERE a."学号" IS NOT NULL AND EXISTS (SELECT 1 FROM {{current}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E168 统计人数：排名表表中，在尚不及格表存在记录的学生

```sql
SELECT COUNT(DISTINCT a."学号") AS "人数" FROM {{ranking}} a WHERE a."学号" IS NOT NULL AND EXISTS (SELECT 1 FROM {{current}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E169 列出学号：排名表表中，在尚不及格表不存在记录的学生

```sql
SELECT DISTINCT a."学号" FROM {{ranking}} a WHERE a."学号" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM {{current}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E170 统计人数：排名表表中，在尚不及格表不存在记录的学生

```sql
SELECT COUNT(DISTINCT a."学号") AS "人数" FROM {{ranking}} a WHERE a."学号" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM {{current}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E171 列出学号：排名表表中，在曾不及格表存在记录的学生

```sql
SELECT DISTINCT a."学号" FROM {{ranking}} a WHERE a."学号" IS NOT NULL AND EXISTS (SELECT 1 FROM {{history}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E172 统计人数：排名表表中，在曾不及格表存在记录的学生

```sql
SELECT COUNT(DISTINCT a."学号") AS "人数" FROM {{ranking}} a WHERE a."学号" IS NOT NULL AND EXISTS (SELECT 1 FROM {{history}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E173 列出学号：排名表表中，在曾不及格表不存在记录的学生

```sql
SELECT DISTINCT a."学号" FROM {{ranking}} a WHERE a."学号" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM {{history}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E174 统计人数：排名表表中，在曾不及格表不存在记录的学生

```sql
SELECT COUNT(DISTINCT a."学号") AS "人数" FROM {{ranking}} a WHERE a."学号" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM {{history}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E175 列出学号：成绩明细表中，在尚不及格表存在记录的学生

```sql
SELECT DISTINCT a."学号" FROM {{detail}} a WHERE a."学号" IS NOT NULL AND EXISTS (SELECT 1 FROM {{current}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E176 统计人数：成绩明细表中，在尚不及格表存在记录的学生

```sql
SELECT COUNT(DISTINCT a."学号") AS "人数" FROM {{detail}} a WHERE a."学号" IS NOT NULL AND EXISTS (SELECT 1 FROM {{current}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E177 列出学号：成绩明细表中，在尚不及格表不存在记录的学生

```sql
SELECT DISTINCT a."学号" FROM {{detail}} a WHERE a."学号" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM {{current}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E178 统计人数：成绩明细表中，在尚不及格表不存在记录的学生

```sql
SELECT COUNT(DISTINCT a."学号") AS "人数" FROM {{detail}} a WHERE a."学号" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM {{current}} b WHERE b."学号" = a."学号");
```

按学号判断是否存在，避免一对多关联放大；不在尚不及格表仅表示快照中无记录，不能直接证明已经补考通过。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E179 各学生当前未通过记录中的不及格次数最大值

```sql
SELECT "学号", MAX(CAST(NULLIF(TRIM(CAST("不及格次数" AS TEXT)), '') AS NUMERIC)) AS "最多次数" FROM {{current}} GROUP BY "学号";
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E180 查询不及格次数至少两次的当前未通过课程记录

```sql
SELECT * FROM {{current}} WHERE CAST(NULLIF(TRIM(CAST("不及格次数" AS TEXT)), '') AS NUMERIC) >= 2;
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E181 按课程统计当前未通过记录的不及格次数平均值

```sql
SELECT "课程号", AVG(CAST(NULLIF(TRIM(CAST("不及格次数" AS TEXT)), '') AS NUMERIC)) AS "平均次数" FROM {{current}} GROUP BY "课程号";
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E182 按学年学期及课程统计曾不及格人数

```sql
SELECT "学年学期", "课程号", COUNT(DISTINCT "学号") AS "人数" FROM {{history}} GROUP BY "学年学期", "课程号";
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E183 按课程属性和修读方式统计成绩记录数

```sql
SELECT "课程属性", "修读方式", COUNT(*) AS "记录数" FROM {{detail}} GROUP BY "课程属性", "修读方式";
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E184 按课程和课序号统计成绩记录人数

```sql
SELECT "课程号", "课序号", COUNT(DISTINCT "学号") AS "人数" FROM {{detail}} GROUP BY "课程号", "课序号";
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E185 列出原表中尚不及格学分大于零的学生

```sql
SELECT "学号", "姓名", CAST(NULLIF(TRIM(CAST("尚不及格学分" AS TEXT)), '') AS NUMERIC) FROM {{ranking}} WHERE CAST(NULLIF(TRIM(CAST("尚不及格学分" AS TEXT)), '') AS NUMERIC) > 0;
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E186 列出曾不及格学分大于零但尚不及格学分为零的学生

```sql
SELECT "学号", "姓名" FROM {{ranking}} WHERE CAST(NULLIF(TRIM(CAST("曾不及格学分" AS TEXT)), '') AS NUMERIC) > 0 AND CAST(NULLIF(TRIM(CAST("尚不及格学分" AS TEXT)), '') AS NUMERIC) = 0;
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E187 计算各学生要求总学分减去已修课程学分的差值

```sql
SELECT "学号", CAST(NULLIF(TRIM(CAST("要求总学分" AS TEXT)), '') AS NUMERIC) - CAST(NULLIF(TRIM(CAST("已修课程学分" AS TEXT)), '') AS NUMERIC) AS "差值" FROM {{ranking}};
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E188 同时查看学生GPA、加权学分成绩和平均成绩

```sql
SELECT "学号", "姓名", CAST(NULLIF(TRIM(CAST("GPA" AS TEXT)), '') AS NUMERIC), CAST(NULLIF(TRIM(CAST("加权学分成绩" AS TEXT)), '') AS NUMERIC), CAST(NULLIF(TRIM(CAST("平均成绩" AS TEXT)), '') AS NUMERIC) FROM {{ranking}};
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E189 按班级统计有尚不及格学分的学生人数

```sql
SELECT "班级", COUNT(DISTINCT "学号") AS "人数" FROM {{ranking}} WHERE CAST(NULLIF(TRIM(CAST("尚不及格学分" AS TEXT)), '') AS NUMERIC) > 0 GROUP BY "班级";
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E190 列出GPA班级排名与加权班级排名不一致的学生

```sql
SELECT "学号", CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA班级排名" AS TEXT)), '/', 1), '') AS NUMERIC), CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权班级排名" AS TEXT)), '/', 1), '') AS NUMERIC) FROM {{ranking}} WHERE CAST(NULLIF(SPLIT_PART(TRIM(CAST("GPA班级排名" AS TEXT)), '/', 1), '') AS NUMERIC) <> CAST(NULLIF(SPLIT_PART(TRIM(CAST("加权班级排名" AS TEXT)), '/', 1), '') AS NUMERIC);
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E191 仅查看重修方式的成绩明细

```sql
SELECT * FROM {{detail}} WHERE "修读方式" = '重修';
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E192 仅查看正常修读的成绩明细

```sql
SELECT * FROM {{detail}} WHERE "修读方式" = '正常';
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E193 统计各课程的重修学生人数

```sql
SELECT "课程号", COUNT(DISTINCT "学号") AS "人数" FROM {{detail}} WHERE "修读方式" = '重修' GROUP BY "课程号";
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E194 按课程属性统计不同课程数

```sql
SELECT "课程属性", COUNT(DISTINCT "课程号") AS "课程数" FROM {{detail}} GROUP BY "课程属性";
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E195 统计各学生涉及多少个历史不及格学年学期

```sql
SELECT "学号", COUNT(DISTINCT "学年学期") AS "学期数" FROM {{history}} GROUP BY "学号";
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E196 查找曾在至少两个学年学期出现不及格记录的学生

```sql
SELECT "学号" FROM {{history}} GROUP BY "学号" HAVING COUNT(DISTINCT "学年学期") >= 2;
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E197 按入学年级和课程统计尚不及格人数

```sql
SELECT "入学年级", "课程号", COUNT(DISTINCT "学号") AS "人数" FROM {{current}} GROUP BY "入学年级", "课程号";
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E198 查询原表GPA缺失的学生

```sql
SELECT "学号", "姓名" FROM {{ranking}} WHERE CAST(NULLIF(TRIM(CAST("GPA" AS TEXT)), '') AS NUMERIC) IS NULL;
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E199 统计各考试时间涉及的不同课程数

```sql
SELECT "考试时间", COUNT(DISTINCT "课程号") AS "课程数" FROM {{detail}} GROUP BY "考试时间";
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。

## E200 比较各学生的加权学分成绩与平均成绩差值

```sql
SELECT "学号", CAST(NULLIF(TRIM(CAST("加权学分成绩" AS TEXT)), '') AS NUMERIC) - CAST(NULLIF(TRIM(CAST("平均成绩" AS TEXT)), '') AS NUMERIC) AS "差值" FROM {{ranking}};
```

按原表字段计算；学分差值不代表官方毕业缺口，缺失值不补零。 数值文本先去空格再转换；排名形如15/1862时只取斜杠前名次，原字段仍保留。
