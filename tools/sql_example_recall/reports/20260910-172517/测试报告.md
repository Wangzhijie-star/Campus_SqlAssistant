# SQL 示例召回测试报告

本轮为初始开发基线，使用实际 `select_training_by_question()`，未修改数据库、向量或运行参数。

## 数据与验证

- 示例200条，全部启用，绑定「学生成绩」，向量768维，异常0条。
- 重新编码核对200条问题，存储向量与当前模型结果最小余弦相似度为 1.00000000，未发现错配。
- 原问题自检200/200命中自身，仅作入库自检，不计入召回率。
- 评测296题：200道单意图改写、48道双意图组合、48道负例。每题的原始结果、文本分支和完整向量排名均保存；296/296与源码回放一致。
- 这是模板合成开发题，没有保留独立验证/测试集。组合题主要覆盖历史/当前挂科和成绩明细，不能代表所有复杂问法。

## 实际配置与结果

阈值0.4，向量Top K 5，保留双向文本包含匹配。

|指标|结果|
|---|---:|
|正例宏平均召回率|83.3%|
|正例宏平均精确率（严格ID）|18.4%|
|完全满足且无额外返回|3.4%（10/296）|
|单意图召回率|92.5%（185/200）|
|双意图全命中率|16.7%（8/48）|
|负例误召回率|79.2%（38/48）|
|域外负例误召回率|58.3%（14/24）|
|同域缺少数据的负例误召回率|100%（24/24）|

召回和精确率先逐题计算，再对248个正例求平均。所有正例均返回5条，单意图只有1条严格标准时精确率上限就是20%；18.4%不能直接解释为其余示例对SQL生成均无参考价值。需业务复核可替代示例集合。
双意图：两项全中8题，仅中一项27题，两项都未中13题。

本批改写和负例均未触发文本包含分支，最终混合结果与向量结果一致。文本分支在原问题自检会命中，因此不能据此认为源码文本分支损坏。

## 向量排名诊断

以下不应用相似度阈值，只观察单意图正确示例在完整向量排名中的位置，不是调整参数后的正式召回率。

|K|单意图Hit@K|
|---|---:|
|1|70.5%|
|3|91.0%|
|5|92.5%|
|10|94.5%|
|20|97.0%|

## 典型情况

### P003 历史挂科记录去重后有几位学生？

标准：曾不及格记录涉及多少名不同学生
实际：E010 按班级统计曾不及格记录涉及的去重学生人数；E018 按课程号统计曾不及格记录涉及的去重学生人数；E020 按课程名统计曾不及格记录涉及的去重学生人数；E078 按课序号统计成绩明细记录涉及的去重学生人数；E088 按班级统计成绩明细记录涉及的去重学生人数
第一个正确示例的向量排名：67

### P009 按班级汇总历史挂科记录的数据条数？

标准：按班级统计曾不及格记录的记录数
实际：E079 按课程名统计成绩明细记录的记录数；E085 按课程属性统计成绩明细记录的记录数；E087 按班级统计成绩明细记录的记录数；E088 按班级统计成绩明细记录涉及的去重学生人数；E107 按班级统计加权排名表学生的记录数
第一个正确示例的向量排名：15

### M001 请分别回答两项：给我展示全部历史挂科记录，依照学生编号排列；另外，历史挂科记录一共有几条数据？

标准：查看全部曾不及格记录，按学号排序；曾不及格记录共有多少行
实际：E001 查看全部曾不及格记录，按学号排序；E069 查看全部成绩明细记录，按学号排序；E072 列出成绩明细记录中出现的学生学号；E074 找出成绩明细记录中有多行记录的学生学号；E106 找出加权排名表学生中有多行记录的学生学号
第一个正确示例的向量排名：5

### N003 如何申请宿舍空调维修？

标准：不返回示例
实际：E102 加权排名表学生共有多少行；E153 查看各学生原表中的已修自主实践学分

### N025 统计每名学生本学期缺勤次数？

标准：不返回示例
实际：E005 统计曾不及格记录中每名学生的记录行数；E007 按学年学期统计曾不及格记录的记录数；E032 找出曾不及格记录中同一学生同一课程出现多次的组合；E068 找出尚不及格记录中同一学生同一课程出现多次的组合；E195 统计各学生涉及多少个历史不及格学年学期

## 后续测试重点

1. 先复核示例标签和组合题。确定需要“完整回答相同查询”还是“可借鉴的SQL模式”，两种目标的相关性标准不同。
2. 在独立的新验证题上评估阈值、Top K和多意图拆分。提高阈值可能减少误召回，但不能解决正确示例排第67位的问题。
3. 200条SQL仍含表名占位符；用于SQL生成前需完成实际表映射。本轮未验证SQL执行结果。

最终证据使用本目录 report.json 及冻结题集。20260910-172337 为修正3道近原文题前的初跑结果，已被本报告替代。
